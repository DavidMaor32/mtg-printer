#include <stdio.h>
#include <string.h>
#include <stdbool.h>

#define equals(str1, str2) (strcmp(str1, str2) == 0)

void get_args(){

}

//global variables
int g_input_cmc;

//flags
bool flag_v = false;


int main(int argc, char** argv){
    // --argc;
    // ++argv;
    // int counter = argc;
    //  while (argc) {
    //     if (equals(*argv, "-v")) {
    //         // Move to the next argument and parse it as an integer
    //         if (--argc && sscanf(*(++argv), "%d", &g_input_cmc) != 1) {
    //             printf("Invalid value for -v\n");
    //         }
    //     }

    //     --argc;
    //     ++argv;
    // }

    

}